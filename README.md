# Project React + Webpack

## Usage

Click `STAR` and `FORK`.

Open your terminal, run: `git clone https://github.com/andyngojs/react-webpack.git`

Run command to install all library: `npm install`

Run project: `npm start`
